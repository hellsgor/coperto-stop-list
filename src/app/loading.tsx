import { MenuListLoading } from '@/features/stop-list/ui/MenuListLoading';

export default function Loading() {
  return (
    <main className="flex flex-1 flex-col">
      <MenuListLoading />
    </main>
  );
}
